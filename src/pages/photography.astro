---
// The photography page moved to /photo-journal. This route stays behind purely
// as a redirect so any link already pointing at /photography keeps working.
return Astro.redirect('/photo-journal', 301);
---
